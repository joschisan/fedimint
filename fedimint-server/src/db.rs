//! Placeholder module retained from the v1 era.
//!
//! The v1 db integrity verification and `ServerInfo` telemetry were removed
//! when the server flipped to the v2 database stack. This file is intentionally
//! minimal and stays only so `pub mod db;` in `lib.rs` still resolves.
